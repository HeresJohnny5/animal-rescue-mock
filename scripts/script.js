/* TYPEDJS SCRIPT */
var options = {
  backDelay: 500,
  backSpeed: 60,
  cursorChar: "|",
  strings: ["Adopt ^1000", "Donate ^1000", "Volunteer ^1000"],
  startDelay: 600,
  stringsElement: null,
  typeSpeed: 60,
  loop: true
}

var typed = new Typed(".typedjs-strings", options);